name: Rebuild inventory, hydrants and offline metadata

on:
  workflow_dispatch:
  push:
    paths:
      - 'Blackwood_CFS_Master_Inventory.xlsx'
      - 'tools/rebuild_inventory_from_excel.py'
      - 'tools/rebuild_directions_hydrants.py'
      - 'directions/hydrants/**'
      - 'directions/index.html'
      - 'directions/source/**'
      - 'photos/**'
      - 'directions/maps/ubd/**'
      - 'manifest.json'
      - 'icon.png'
      - 'icons/**'

permissions:
  contents: write

jobs:
  rebuild-app:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Rebuild Directions hydrant links
        run: python tools/rebuild_directions_hydrants.py --geocode-missing

      - name: Rebuild inventory and offline metadata
        run: python tools/rebuild_inventory_from_excel.py

      - name: Check for generated changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit rebuilt app files
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html data/inventory.json content-metadata.json offline-assets.json \
            directions/index.html directions/hydrants/geocodes.json \
            directions/hydrants/GEOCODING_REVIEW_CURRENT.csv
          git commit -m "Auto-rebuild inventory, hydrants and offline metadata"
          git push
