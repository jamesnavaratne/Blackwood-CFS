# Blackwood CFS v2.4.2 Directions Report-Pill Source-Row Test

Base: `Blackwood_CFS_v2.4.1_DIRECTIONS_ISSUE_REPORTING_TEST.zip`

Purpose:
- Move the Directions Book `Report issue` pill into the same row as the `SOURCE:` line.
- Make the pill slightly smaller.
- Keep the issue-report modal behaviour unchanged.

Changed:
- `directions/index.html`
- `service-worker.js` cache name only
- release/verification notes

Unchanged:
- Directions data/fidelity
- Inventory data
- Photos
- UBD map files
- Inventory rebuild tools
